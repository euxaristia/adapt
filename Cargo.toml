[package]
name = "adapter"
version = "0.1.0"
edition = "2021"
authors = ["You"]
description = "A paru-like wrapper for apt"
license = "MIT"

[dependencies]
clap = { version = "4.5", features = ["derive", "cargo"] }
anyhow = "1.0"

[profile.release]
opt-level = 3
lto = true
strip = true
